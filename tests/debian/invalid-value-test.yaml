meta:
  name: debian-invalid-value
  tags: 
    - bookworm
  from: debian:bookworm

layer:
  manager:
    name: apt
    options:
      install-recommends: "yes"    # Should be "true" or "false"
  actions:
    install:
      packages:
        - curl

publish:
  - type: local
