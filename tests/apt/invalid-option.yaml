meta:
  name: invalid-option
  tags: 
    - "test"
  from: scratch

layer:
  manager:
    name: mmdebstrap
    options:
      suite: "bookworm"
      mirror: "http://deb.debian.org/debian"
      invalid_option: "should_fail"
  actions:
    install:
      packages:
        - bash

publish:
  - type: local
