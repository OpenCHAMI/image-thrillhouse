meta:
  name: debian-base
  tag: bookworm
  from: scratch

layer:
  manager:
    name: mmdebstrap
    options:
      suite: bookworm
      mirror: http://deb.debian.org/debian
      variant: minbase
      mode: fakechroot
  actions:
    install:
      packages:
        - ssh
        - vim
publish:
  - type: local
