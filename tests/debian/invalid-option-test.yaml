meta:
  name: debian-invalid-option
  tags: 
    - bookworm
  from: debian:bookworm

layer:
  manager:
    name: apt
    options:
      invalid-option: "true"    # This should fail validation
  actions:
    install:
      packages:
        - curl

publish:
  - type: local
