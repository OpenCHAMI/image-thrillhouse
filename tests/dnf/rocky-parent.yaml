meta:
  name: rocky-parent-basic
  tags:
    - "9-custom"
  from: docker://rockylinux:9

layer:
  manager:
    name: dnf
  actions:
    install:
      packages:
        - vim
        - wget
        - git

publish:
  - type: local
